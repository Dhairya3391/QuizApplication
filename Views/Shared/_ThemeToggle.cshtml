<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>@ViewData["Title"] - YourAppName</title>
    
    
    <style>
        /* Dark Mode Variables */
        :root {
          --light-bg: #f6f9ff;
          --light-text: #444444;
          --light-heading: #012970;
          --light-sidebar: #fff;
          --light-card: #fff;
          --light-border: #ebeef4;
          --light-link: #4154f1;
          --light-link-hover: #717ff5;
          --light-muted: #899bbd;
          --light-active-sidebar: #f0f0f0;
          --light-active-sidebar-text: #000;
        }

        /* Dark Mode Theme */
        .dark {
          --bs-body-bg: #222;
          --bs-body-color: #e4e6eb;
          background-color: #222 !important;
          color: #e4e6eb !important;
        }

        .dark body {
          background: #1a1a1a;
          color: #e4e6eb;
        }

        .dark a {
          color: #6c8fff;
        }

        .dark a:hover {
          color: #8ca3ff;
        }

        .dark h1, .dark h2, .dark h3, .dark h4, .dark h5, .dark h6 {
          color: #e4e6eb;
        }

        /* Main Content */
        .dark #main {
          background-color: #1a1a1a;
        }

        .dark .pagetitle h1 {
          color: #e4e6eb;
        }

        /* Header */
        .dark .header {
          background-color: #222;
          box-shadow: 0px 2px 20px rgba(0, 0, 0, 0.4);
        }

        .dark .header .toggle-sidebar-btn {
          color: #e4e6eb;
        }

        .dark .logo span {
          color: #e4e6eb;
        }

        .dark .header .search-form input {
          color: #e4e6eb;
          background-color: #333;
          border: 1px solid #444;
        }

        .dark .header .search-form input:focus,
        .dark .header .search-form input:hover {
          border: 1px solid #555;
          box-shadow: 0 0 10px 0 rgba(255, 255, 255, 0.1);
        }

        .dark .header .search-form button i {
          color: #e4e6eb;
        }

        /* Header Nav */
        .dark .header-nav .nav-icon {
          color: #e4e6eb;
        }

        .dark .header-nav .nav-profile {
          color: #e4e6eb;
        }

        .dark .header-nav .notifications .notification-item:hover,
        .dark .header-nav .messages .message-item:hover {
          background-color: #333;
        }

        .dark .header-nav .profile .dropdown-header h6 {
          color: #e4e6eb;
        }

        .dark .header-nav .profile .dropdown-item:hover {
          background-color: #333;
        }

        /* Sidebar */
        .dark .sidebar {
          background-color: #222;
          box-shadow: 0px 0px 20px rgba(0, 0, 0, 0.4);
        }

        .dark .sidebar::-webkit-scrollbar {
          background-color: #222;
        }

        .dark .sidebar::-webkit-scrollbar-thumb {
          background-color: #444;
        }

        .dark .sidebar-nav .nav-link {
          color: #6c8fff;
          background: #2a2a2a;
        }

        .dark .sidebar-nav .nav-link.collapsed {
          color: #e4e6eb;
          background: #222;
        }

        .dark .sidebar-nav .nav-link.collapsed i {
          color: #aaa;
        }

        .dark .sidebar-nav .nav-link:hover {
          color: #6c8fff;
          background: #2a2a2a;
        }

        .dark .sidebar-nav .nav-content a {
          color: #e4e6eb;
        }

        .dark .sidebar-nav .nav-content a:hover,
        .dark .sidebar-nav .nav-content a.active {
          color: #6c8fff;
        }

        .dark .sidebar-nav .nav-link.active {
          background-color: #333;
          color: #fff;
        }

        /* Cards */
        .dark .card {
          background-color: #222;
          box-shadow: 0px 0 30px rgba(0, 0, 0, 0.4);
        }

        .dark .card-header,
        .dark .card-footer {
          border-color: #333;
          background-color: #222;
          color: #aaa;
        }

        .dark .card-title {
          color: #e4e6eb;
        }

        .dark .card-title span {
          color: #aaa;
        }

        /* Dropdown menus */
        .dark .dropdown-menu {
          background-color: #222;
          box-shadow: 0 5px 30px 0 rgba(0, 0, 0, 0.4);
        }

        .dark .dropdown-menu .dropdown-header,
        .dark .dropdown-menu .dropdown-footer {
          color: #e4e6eb;
        }

        .dark .dropdown-menu .dropdown-footer a {
          color: #aaa;
        }

        .dark .dropdown-menu .dropdown-divider {
          border-color: #333;
        }

        .dark .dropdown-menu .dropdown-item {
          color: #e4e6eb;
        }

        .dark .dropdown-menu .dropdown-item:hover {
          background-color: #333;
        }

        .dark .dropdown-menu-arrow::before {
          background: #222;
          border-top: 1px solid #333;
          border-left: 1px solid #333;
        }

        /* Breadcrumbs */
        .dark .breadcrumb {
          color: #aaa;
        }

        .dark .breadcrumb a {
          color: #aaa;
        }

        .dark .breadcrumb a:hover {
          color: #e4e6eb;
        }

        .dark .breadcrumb .breadcrumb-item::before {
          color: #aaa;
        }

        .dark .breadcrumb .active {
          color: #e4e6eb;
        }

        /* Tabs */
        .dark .nav-tabs-bordered {
          border-bottom: 2px solid #333;
        }

        .dark .nav-tabs-bordered .nav-link {
          color: #aaa;
        }

        .dark .nav-tabs-bordered .nav-link:hover,
        .dark .nav-tabs-bordered .nav-link:focus {
          color: #6c8fff;
        }

        .dark .nav-tabs-bordered .nav-link.active {
          background-color: #222;
          color: #6c8fff;
          border-bottom: 2px solid #6c8fff;
        }

        /* Form Controls */
        .dark input, 
        .dark select, 
        .dark textarea, 
        .dark .form-control, 
        .dark .form-select {
          background-color: #333;
          border-color: #444;
          color: #e4e6eb;
        }

        .dark input:focus, 
        .dark select:focus, 
        .dark textarea:focus, 
        .dark .form-control:focus, 
        .dark .form-select:focus {
          background-color: #333;
          border-color: #6c8fff;
          color: #e4e6eb;
        }

        /* Tables */
        .dark .table {
          color: #e4e6eb;
          border-color: #333;
        }

        .dark .table thead {
          background-color: #2a2a2a;
        }

        .dark .table thead th {
          color: #e4e6eb;
        }

        .dark .table tbody tr:hover {
          background-color: #2a2a2a;
        }

        .dark .table-bordered {
          border-color: #333;
        }

        .dark .table-bordered th,
        .dark .table-bordered td {
          border-color: #333;
        }

        /* Footer */
        .dark .footer {
          border-top: 1px solid #333;
        }

        .dark .footer .copyright,
        .dark .footer .credits {
          color: #aaa;
        }

        /* Toggle Button Styling */
        .theme-toggle-wrapper {
          display: flex;
          align-items: center;
          margin-right: 15px;
        }

        .theme-toggle {
          position: relative;
          display: inline-block;
          width: 60px;
          height: 30px;
        }

        .theme-toggle input {
          opacity: 0;
          width: 0;
          height: 0;
        }

        .theme-toggle-slider {
          position: absolute;
          cursor: pointer;
          top: 0;
          left: 0;
          right: 0;
          bottom: 0;
          background-color: #ccc;
          transition: .4s;
          border-radius: 34px;
        }

        .theme-toggle-slider:before {
          position: absolute;
          content: "";
          height: 22px;
          width: 22px;
          left: 4px;
          bottom: 4px;
          background-color: white;
          transition: .4s;
          border-radius: 50%;
        }

        input:checked + .theme-toggle-slider {
          background-color: #6c8fff;
        }

        input:focus + .theme-toggle-slider {
          box-shadow: 0 0 1px #6c8fff;
        }

        input:checked + .theme-toggle-slider:before {
          transform: translateX(30px);
        }

        .theme-toggle-slider:after {
          content: "☀️";
          position: absolute;
          left: 8px;
          top: 6px;
          font-size: 14px;
        }

        input:checked + .theme-toggle-slider:after {
          content: "🌙";
          left: auto;
          right: 8px;
        }
    </style>
    
    <!-- Other head elements -->
</head>
<body><!-- Add the theme toggle before your other nav items -->
                <li class="nav-item">
                    <div class="theme-toggle-wrapper">
                        <label class="theme-toggle">
                            <input type="checkbox" id="theme-toggle">
                            <span class="theme-toggle-slider"></span>
                        </label>
                    </div>
                </li>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Get the toggle checkbox
            const themeToggle = document.getElementById('theme-toggle');
            
            // Check for saved theme preference or respect OS preference
            const prefersDarkScheme = window.matchMedia('(prefers-color-scheme: dark)');
            const currentTheme = localStorage.getItem('theme');
            
            // If the user has explicitly chosen a theme, use it
            if (currentTheme) {
                document.documentElement.classList.toggle('dark', currentTheme === 'dark');
                themeToggle.checked = currentTheme === 'dark';
            } 
            // Otherwise, respect the OS preference
            else if (prefersDarkScheme.matches) {
                document.documentElement.classList.add('dark');
                themeToggle.checked = true;
            }
            
            // Listen for toggle changes
            themeToggle.addEventListener('change', function() {
                if (this.checked) {
                    document.documentElement.classList.add('dark');
                    localStorage.setItem('theme', 'dark');
                } else {
                    document.documentElement.classList.remove('dark');
                    localStorage.setItem('theme', 'light');
                }
            });
          
            prefersDarkScheme.addEventListener('change', function(e) {
                if (!localStorage.getItem('theme')) {
                    if (e.matches) {
                        document.documentElement.classList.add('dark');
                        themeToggle.checked = true;
                    } else {
                        document.documentElement.classList.remove('dark');
                        themeToggle.checked = false;
                    }
                }
            });
        });
    </script>
</body>
</html>

